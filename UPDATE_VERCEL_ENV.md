# 🔄 Обновление DATABASE_URL в Vercel

## Переменная уже существует - нужно обновить

Если вы видите ошибку: *"A variable with the name `DATABASE_URL` already exists"*

Это значит, что переменная уже есть, но нужно обновить её значение на новый connection string от Supabase.

---

## Шаги для обновления:

### 1. Откройте Vercel Dashboard

1. Перейдите на: https://vercel.com/dashboard
2. Выберите проект `SmartTasbihGoals`
3. Перейдите в **Settings** → **Environment Variables**

### 2. Найдите существующий DATABASE_URL

Вы увидите список переменных. Найдите `DATABASE_URL`.

### 3. Обновите значение

1. **Нажмите на `DATABASE_URL`** (или кнопку "Edit" рядом с ним)
2. **Измените Value** на:
   ```
   postgresql://postgres:Sebiev9595@db.xdlkilvotcnssarzugws.supabase.co:5432/postgres?sslmode=require
   ```
3. Убедитесь, что выбраны **Production**, **Preview**, и **Development**
4. **Нажмите "Save"**

### 4. Передеплойте проект

После обновления переменной:

1. Перейдите в **Deployments**
2. Найдите последний деплой
3. Нажмите **"..."** (три точки) → **"Redeploy"**
4. Подождите завершения деплоя

---

## Альтернативный способ (если не можете найти Edit)

Если не видите кнопку Edit:

1. **Удалите** старую переменную `DATABASE_URL`
2. **Добавьте** новую с тем же именем и новым значением

⚠️ **Внимание:** После удаления/добавления переменной нужно **обязательно передеплоить** проект!

---

## Проверка после обновления

1. После передеплоя проверьте логи:
   - Vercel Dashboard → **Functions** → **Logs**
   - Должно быть: `✅ Подключение к базе данных установлено`

2. Проверьте приложение:
   - Откройте ваше приложение на Vercel
   - Не должно быть ошибок 500
   - API запросы должны работать

---

## Новый Connection String для Supabase:

```
postgresql://postgres:Sebiev9595@db.xdlkilvotcnssarzugws.supabase.co:5432/postgres?sslmode=require
```

**Важно:** Убедитесь, что в конце есть `?sslmode=require`

