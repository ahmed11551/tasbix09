# 💡 Советы по оптимизации деплоев на Vercel

## Проблема: Лимит деплоев (100 в день)

Если вы получили ошибку:
```
Ресурс ограничен — повторите попытку через 4 часа 
(более 100, код: «api-deployments-free-per-day»)
```

Это означает, что вы достигли лимита бесплатного плана Vercel: **100 деплоев в день**.

## Решения

### 1. Подождать (самое простое) ⏰
- Подождите 4 часа или до начала следующего дня
- Лимит сбрасывается каждые 24 часа

### 2. Настроить деплой только для важных веток 🎯

В настройках Vercel проекта:
1. Перейдите в **Settings → Git**
2. Настройте **Production Branch**: `main` или `master`
3. Отключите авто-деплой для других веток

Или создайте `.vercelignore` для игнорирования изменений:
```gitignore
# .vercelignore
README.md
*.md
docs/
```

### 3. Использовать GitHub Actions для условного деплоя 🚀

Создайте `.github/workflows/deploy.yml`:
```yaml
name: Deploy to Vercel
on:
  push:
    branches: [main]
    paths-ignore:
      - '**.md'
      - '.gitignore'
      - 'README.md'
      - 'docs/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Deploy to Vercel
        uses: amondnet/vercel-action@v25
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.ORG_ID }}
          vercel-project-id: ${{ secrets.PROJECT_ID }}
          vercel-args: '--prod'
```

### 4. Объединять несколько коммитов в один 🔀

Вместо множества мелких коммитов, делайте один большой:
```bash
git add .
git commit -m "feat: add multiple features at once"
git push
```

### 5. Использовать Previews только для важных PR 👀

В настройках Vercel:
- Отключите автоматические preview деплои
- Включайте их только для критичных изменений

### 6. Перейти на Pro план 💼

Если вам нужно больше деплоев:
- Vercel Pro: $20/месяц
- Unlimited деплоев
- Дополнительные функции

## Рекомендации

✅ **Делайте:**
- Коммитьте и деплойте только готовые изменения
- Используйте feature branches локально, объединяйте в main
- Деплойте после тестирования локально

❌ **Не делайте:**
- Множество мелких коммитов с авто-деплоем
- Деплой при каждом сохранении
- Деплой для документации/README изменений

## Проверка текущего статуса

В Vercel Dashboard → Deployments вы можете увидеть:
- Сколько деплоев сделано сегодня
- Когда лимит сбросится

## Альтернативные решения

Если нужно больше деплоев:
1. **Vercel Pro** - $20/месяц, unlimited деплоев
2. **Self-hosted** - используйте Docker и свой сервер
3. **Другие платформы** - Netlify, Railway, Render (у них другие лимиты)

---

**Совет:** Для разработки используйте локальный сервер (`npm run dev`), деплойте только готовые версии.

