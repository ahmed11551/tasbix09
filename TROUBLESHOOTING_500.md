# 🔧 Устранение ошибок 500

## Проблема: Все API возвращают 500 ошибку

Это означает, что есть проблема с подключением к базе данных или таблицы не созданы.

---

## ✅ Пошаговая проверка

### Шаг 1: Проверьте переменные окружения в Vercel

1. Откройте: https://vercel.com/dashboard
2. Выберите проект `SmartTasbihGoals`
3. Перейдите в **Settings** → **Environment Variables**
4. Убедитесь, что есть:
   - ✅ `DATABASE_URL` - должен содержать ваш connection string
   - ✅ `SESSION_SECRET` - случайная строка минимум 32 символа

### Шаг 2: Проверьте логи Vercel

1. В Vercel Dashboard → ваш проект
2. Перейдите в **Functions** → **Logs**
3. Найдите последние ошибки
4. Проверьте сообщения:
   - `❌ DATABASE_URL не установлен!` → Добавьте переменную
   - `❌ Ошибка подключения к базе данных` → Проверьте connection string
   - `Table does not exist` → Миграции не применены

---

## 🔧 Решения

### Проблема 1: DATABASE_URL не настроен

**Решение:**
1. В Vercel → Settings → Environment Variables
2. Добавьте:
   ```
   Key: DATABASE_URL
   Value: postgresql://neondb_owner:npg_MSvb1ZBm8dqI@ep-soft-moon-ahu70lnh-pooler.c-3.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
   ```
3. Передеплойте проект

---

### Проблема 2: Таблицы не созданы (миграции не применены)

**Решение A: Через Neon SQL Editor (рекомендуется)**

1. Откройте: https://console.neon.tech
2. Выберите проект `sparkling-sound-64172647`
3. Перейдите в **SQL Editor**
4. Откройте файл `prisma/init-database.sql` из проекта
5. Скопируйте весь SQL код
6. Вставьте в SQL Editor
7. Нажмите **"Run"** или **"Execute"**

**Решение B: Через Prisma CLI**

```bash
# 1. Установите Vercel CLI
npm i -g vercel

# 2. Войдите
vercel login

# 3. Перейдите в папку проекта
cd /Users/ahmeddevops/Desktop/SmartTasbihGoals_pub-main

# 4. Скачайте переменные окружения
vercel env pull .env.local

# 5. Проверьте, что DATABASE_URL есть в .env.local
cat .env.local

# 6. Примените миграции
npx prisma migrate deploy
```

---

### Проблема 3: Connection string неправильный

**Проверка:**
1. Connection string должен начинаться с `postgresql://`
2. Должен содержать `?sslmode=require`
3. Не должен содержать `localhost` или `127.0.0.1`

**Правильный формат:**
```
postgresql://username:password@host:port/database?sslmode=require
```

---

### Проблема 4: База данных в режиме "sleep"

Neon может переводить БД в режим сна на бесплатном плане.

**Решение:**
1. Откройте Neon Dashboard
2. Выберите ваш проект
3. Откройте SQL Editor
4. Выполните любой запрос (например, `SELECT 1;`)
5. Это "разбудит" базу данных

---

## 🧪 Быстрая проверка

### Тест 1: Проверка подключения к БД

Выполните в Neon SQL Editor:
```sql
SELECT version();
```

Если запрос выполняется - база данных работает.

### Тест 2: Проверка таблиц

Выполните в Neon SQL Editor:
```sql
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public';
```

Должны быть таблицы: `User`, `Habit`, `Task`, `Goal`, `Session`, `DhikrLog`, и т.д.

Если таблиц нет - примените миграции (см. Проблема 2).

---

## 📝 Чеклист

- [ ] DATABASE_URL добавлен в Vercel Environment Variables
- [ ] SESSION_SECRET добавлен в Vercel Environment Variables
- [ ] Переменные добавлены для Production, Preview, Development
- [ ] Проект передеплоен после добавления переменных
- [ ] SQL миграции применены (таблицы созданы)
- [ ] База данных активна (не в режиме sleep)
- [ ] Connection string правильного формата

---

## 🆘 Если ничего не помогает

1. **Проверьте логи Vercel** - там будет точная ошибка
2. **Проверьте логи Neon** - есть ли ошибки подключения
3. **Создайте тестового пользователя** через Neon SQL Editor:
   ```sql
   INSERT INTO "User" (id, username, password, "subscriptionTier") 
   VALUES ('test-user-id', 'testuser', 'hashed_password', 'muslim');
   ```

---

После исправления всех проблем API запросы должны вернуть 200 вместо 500.

