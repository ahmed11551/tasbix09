# 🔍 Диагностика ошибок 500

## Срочно проверить логи Vercel

Ошибки 500 означают, что что-то не так на сервере. Нужно посмотреть логи, чтобы узнать точную причину.

---

## Шаг 1: Проверить логи Vercel

1. **Откройте**: https://vercel.com/dashboard
2. **Выберите проект** `SmartTasbihGoals`
3. Перейдите в **Functions** → **Logs**
4. Найдите последние ошибки (они будут красными)

### Что искать в логах:

**Ошибка 1: DATABASE_URL не установлен**
```
❌ DATABASE_URL не установлен в переменных окружения Vercel!
```
→ **Решение:** Добавьте `DATABASE_URL` в Environment Variables

**Ошибка 2: Не удалось подключиться к БД**
```
❌ Ошибка подключения к базе данных
Can't reach database server
```
→ **Решение:** Проверьте connection string и доступность Supabase

**Ошибка 3: Таблицы не найдены**
```
Table "User" does not exist
relation "User" does not exist
```
→ **Решение:** Примените миграции в Supabase SQL Editor

**Ошибка 4: Неверный формат connection string**
```
invalid connection string
```
→ **Решение:** Проверьте формат connection string

---

## Шаг 2: Проверить переменные окружения

1. В Vercel Dashboard → Settings → **Environment Variables**
2. Убедитесь, что `DATABASE_URL` существует
3. **Скопируйте значение** и проверьте, что оно содержит:
   ```
   postgresql://postgres:Sebiev9595@db.xdlkilvotcnssarzugws.supabase.co:5432/postgres?sslmode=require
   ```

⚠️ **Важно:** После изменения переменных **обязательно передеплойте проект!**

---

## Шаг 3: Проверить подключение к Supabase

Проверим, что база данных доступна:

1. Откройте Supabase Dashboard: https://supabase.com/dashboard
2. Выберите ваш проект
3. Перейдите в **SQL Editor**
4. Выполните запрос:
   ```sql
   SELECT COUNT(*) FROM "User";
   ```
   
   Если запрос выполняется без ошибок - база данных работает.

---

## Шаг 4: Передеплойть проект

После проверки и исправления:

1. В Vercel Dashboard → **Deployments**
2. Найдите последний деплой
3. Нажмите **"..."** → **"Redeploy"**
4. **Подождите завершения деплоя** (1-2 минуты)

---

## Возможные причины ошибок 500

### 1. DATABASE_URL не обновлен
- Проверьте значение в Vercel Environment Variables
- Убедитесь, что вы обновили, а не создали новую переменную
- **Передеплойте** после изменения

### 2. Проект не передеплоен
- Старый деплой все еще использует старую переменную
- Нужно передеплоить после изменения Environment Variables

### 3. Неверный формат connection string
- Должен начинаться с `postgresql://`
- Должен содержать `?sslmode=require` в конце
- Пароль должен быть правильным

### 4. База данных недоступна
- Проверьте Supabase Dashboard - проект должен быть активен
- Попробуйте выполнить SQL запрос в Supabase SQL Editor

---

## 📋 Чеклист проверки

- [ ] Логи Vercel проверены - найдена точная ошибка
- [ ] DATABASE_URL существует в Environment Variables
- [ ] Значение DATABASE_URL правильное (Supabase connection string)
- [ ] Проект передеплоен после изменения переменных
- [ ] Supabase база данных доступна (проверено через SQL Editor)
- [ ] Таблицы созданы в Supabase (12 таблиц)

---

## 🆘 Если ничего не помогает

Пришлите **текст ошибки из логов Vercel** - там будет точная причина проблемы.

**Как получить ошибку:**
1. Vercel Dashboard → Functions → Logs
2. Найдите красную строку с ошибкой
3. Скопируйте весь текст ошибки

